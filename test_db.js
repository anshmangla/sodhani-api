const { Pool } = require('pg'); const pool = new Pool({ connectionString: 'postgres://sodhani:sodhani_local_pass@localhost:5432/sodhaniscrap' }); pool.query('SELECT "FinInstrmId" FROM company_stock WHERE TRIM(UPPER("TckrSymb")) = TRIM(UPPER(\)) OR TRIM("FinInstrmId"::text) = TRIM(\) LIMIT 1', ['INTLCOMBQ']).then(res => { console.log(res.rows); pool.end(); }).catch(e => console.error('Error:', e));
